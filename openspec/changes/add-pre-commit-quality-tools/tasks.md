## 1. Tools/ scaffolding

- [x] 1.1 Create `tools/phpstan/composer.json` requiring `phpstan/phpstan ^2.0`, `phpstan/phpstan-symfony ^2.0`, `phpstan/phpstan-doctrine ^2.0`. Set `config.sort-packages: true`. No `autoload` block. (Also pulled in `phpstan/phpstan-phpunit` since the project uses PHPUnit attributes that the extension understands.)
- [x] 1.2 Create `tools/php-cs-fixer/composer.json` requiring `friendsofphp/php-cs-fixer ^3.0`.
- [x] 1.3 Create `tools/rector/composer.json` requiring `rector/rector ^2.0`.
- [x] 1.4 Add `tools/*/vendor/` to `.gitignore`. Confirm `tools/*/composer.lock` files ARE tracked (the per-tool lockfiles are committed).
- [x] 1.5 Run `composer install -d tools/phpstan && composer install -d tools/php-cs-fixer && composer install -d tools/rector` to populate vendors and generate the three lockfiles. Stage the lockfiles.

## 2. Project-root configuration files

- [x] 2.1 Create `phpstan.dist.neon` at level 6 with `paths: [src, tests]` and `excludePaths: [vendor, var, tools, migrations]`. Include `phpstan-symfony` + `phpstan-doctrine` + `phpstan-phpunit` extensions. (Container-aware introspection deferred until baseline is stable.)
- [x] 2.2 Create `.php-cs-fixer.dist.php` at the repo root: rules `@Symfony` + `@Symfony:risky` + a small project-specific override block. Finder excludes `vendor/`, `var/`, `tools/`, `migrations/`, `public/bundles/`, `public/assets/`.
- [x] 2.3 Create `rector.php` at the repo root: PHP 8.5 set + Symfony code-quality set, paths `[src, tests]`. Trim unused imports.

## 3. Top-level composer scripts

- [x] 3.1 Add to `composer.json` `scripts` block: `tools:install`, `tools:update`, `phpstan`, `cs:check`, `cs:fix`, `rector`, `rector:dry`, `quality`. Each runs the isolated `tools/<tool>/vendor/bin/<binary>` directly. `quality` chains `cs:check` and `phpstan`.
- [x] 3.2 Add a `post-install-cmd` hook entry that prints (only on TTY, only when `core.hooksPath` is unset or not pointing at `.githooks`) a one-line activation hint. Implemented as `App\Composer\Hooks::printDevHints` (a class is more readable than `php -r` inline and easier to test/extend).
- [x] 3.3 Verify the scripts work locally: tool binaries respond to `--version` (PHPStan 2.1.54, PHP-CS-Fixer 3.95.1, Rector 2.4.2). `composer cs:check` reports diffs (handled in Section 5).

## 4. Pre-commit hook

- [x] 4.1 Create `.githooks/pre-commit` (executable shell script): identifies staged `.php` files via `git diff --cached --name-only --diff-filter=ACM | grep '\.php$'`. If any: run PHP-CS-Fixer in fix mode against just those files, re-stage with `git add` if anything changed.
- [x] 4.2 In the same hook: run `composer phpstan` against the whole project. Abort the commit on non-zero exit, printing PHPStan's output unmodified.
- [x] 4.3 Hook fails fast if `tools/phpstan/vendor/bin/phpstan` or `tools/php-cs-fixer/vendor/bin/php-cs-fixer` are missing — print "run `composer tools:install`" and exit 1 without prompting.
- [x] 4.4 `chmod +x .githooks/pre-commit` and stage it executable.

## 5. Make existing code pass the tools

- [x] 5.1 Run `composer cs:fix` against the whole project. ~55 files reformatted across `src/`, `tests/`, `config/`, plus the project-root scripts. Tests stay green (420 unit + 153 component + 186 functional). `config/reference.php` excluded as autogenerated.
- [x] 5.2 Run `composer phpstan`. 236 findings at level 6; baselined to `phpstan-baseline.neon` and included from `phpstan.dist.neon`. New code does not contribute to the baseline; entries are removed as the codebase catches up.
- [x] 5.3 Run `composer rector:dry`. 155 files would change — most are import-ordering, type-narrowing, and PHP-85 modernisation. Not applied here per Decision 8 (Rector is on-demand only). Recorded as a future cleanup opportunity.

## 6. Dependabot

- [x] 6.1 (Existing root `.github/dependabot.yml` extended with) one `composer` entry per `tools/<tool>/` directory: `/tools/phpstan`, `/tools/php-cs-fixer`, `/tools/rector`. Same weekly Monday Europe/Berlin schedule as the root entry; `open-pull-requests-limit: 5` per tool; labels `dependencies`, `php`, `tools`.
- [x] 6.2 Existing `.github/dependabot.yml` was extended in place — root composer + github-actions entries preserved verbatim.
- [ ] 6.3 Verify the YAML parses on the GitHub side — push to a branch and check that GitHub's Dependabot tab lists all four `composer` entries plus `github-actions`.

## 7. CI integration

- [x] 7.1 New `quality` job in `.github/workflows/ci.yml` runs `composer install` (root) → `composer tools:install` → `composer quality`. Runs in parallel to the existing `test` job.
- [x] 7.2 PHP-CS-Fixer's `--diff` output and PHPStan's grouped error output both flow through unchanged — no GitHub-Actions-specific reporter wrapping. CI log preserves the same format developers see locally.
- [ ] 7.3 Smoke test the CI run on a branch — first push of this change exercises the quality job in real CI. Defer to the actual push.

## 8. Documentation

- [x] 8.1 README "Quality stack" section added: tool table, one-time setup (`composer tools:install` + `git config core.hooksPath .githooks`), day-to-day commands, baseline note.
- [x] 8.2 CONTRIBUTING.md "Quality stack" section added with the day-to-day commands; "Adding a new quality tool" subsection captures the four-step checklist (drop tools/<name>/, generate lockfile, add Dependabot entry, add composer scripts) including the Dependabot-is-mandatory rule from the spec.
- [x] 8.3 CONTRIBUTING.md "Conventions" gained a "Quality gate before commit" line pointing at `composer quality` and the hook activation. (Note: the global CLAUDE.md still says "run make lint" — that's a per-developer note, not a project doc; not in scope here.)

## 9. Verification

- [~] 9.1 Fresh-clone smoke test — deferred to follow-up when the change pushes; verified locally in this clone (`composer cs:check`, `composer phpstan`, `composer quality` all clean from a populated tools/ tree).
- [~] 9.2 Hook smoke test — deferred. Manually inspecting the hook script confirms the logic; a real-world test happens on the first push of this change.
- [ ] 9.3 CI smoke test: open a no-op PR; confirm the new quality step runs and passes. (Will exercise on the first push of this change.)
- [ ] 9.4 Dependabot smoke test: push the change to a branch, wait for GitHub to evaluate the new `.github/dependabot.yml`. Confirm no syntax errors in the Dependabot tab.

## 10. Deferred / future

- [ ] 10.1 Coverage tooling (pcov + threshold) — out of scope here; lands as its own change when prioritised.
- [ ] 10.2 Mutation testing (Infection) — same.
- [ ] 10.3 Pre-push hook running `composer test:unit` — different feedback loop, distinct concerns.
- [ ] 10.4 Auto-update via Dependabot grouping rules (group all phpstan-* updates into one PR) — viable optimisation once we've observed actual update churn.
